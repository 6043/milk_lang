n = input
i = 0

while i < n do {
    sqrt = 2
    squared = sqrt ** 2
    while squared < i do {
        sqrt = sqrt + 1
        squared = sqrt ** 2
    }
    factor = 2
    is_prime = true
    while factor < sqrt do {
        remainder = i % factor
        if remainder == 0 then {
            is_prime = false
            break
        }
        factor = factor + 1
    }
    if is_prime == true then {
        moo i
    }
    i = i + 1
}